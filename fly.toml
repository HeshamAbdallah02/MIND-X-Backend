app = "your-app-name"
primary_region = "iad"  # Choose your region

[build]
  [build.args]
    NODE_ENV = "production"

[env]
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true